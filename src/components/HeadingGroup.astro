---
const { class: className, heading, subheading, body } = Astro.props;
---

<hgroup
  class={className +
    " flex flex-col gap-1 after:w-24 after:h-0.5 after:bg-primary-dark after:mt-4 after:rounded-sm text-pretty"}>
  <h1 class="font-exteded text-3xl tracking-tight">
    {heading}
  </h1>

  {
    subheading && (
      <h2 class="text-pretty font-mono text-lg tracking-tighter text-muted">
        {subheading}
      </h2>
    )
  }

  {body && <p class="text-lg">{body}</p>}
</hgroup>
